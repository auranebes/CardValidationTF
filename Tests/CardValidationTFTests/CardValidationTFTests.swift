import XCTest
@testable import CardValidationTF

final class CardValidationTFTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CardValidationTF().text, "Hello, World!")
    }
}
